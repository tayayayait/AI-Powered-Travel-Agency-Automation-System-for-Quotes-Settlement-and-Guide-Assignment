# UI/UX 구현 상세서 (동남아 랜드사 통합 운영 시스템 / Phase 1~3)

## 0. 문서 요약(결정 사항)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| 적용 스킬 | `ui-ux-pro-max` → `writing-clearly-and-concisely` → `prompt-engineering` | 본 상세서 작성 기준 | 접근성/반응형/상태 설계 우선, 규칙은 짧고 실행 가능하게 작성, 표 템플릿 일관 유지 |
| 기본 UI 모드 | Light (다크모드 미포함) | 전 화면 | 다크모드는 추후 토큰 확장으로 대응(Assumptions 참조) |
| 최우선 UX 목표 | “입력→검증→승인→출력” 1 플로우 5분 내 완주 | Phase 1 견적 | 자동계산·자동검증·승인 전 외부반출 차단(버튼/출력 비활성) |
| 정보 구조(PC) | 좌측 Sidebar + 상단 Topbar + 본문 Workspace | 관리자/내근 대시보드 | 테이블/필터 중심, 빠른 검색·단축행동 우선 |
| 정보 구조(모바일) | 상단 Topbar + 본문 + 하단 고정 Action bar | 가이드/현지 소장 폼 | 터치 타겟 44×44px, 선택형 입력 우선(수기 계산 금지) |
| 상태 체계(공통) | Draft / InReview / Approved / Rejected / Locked | 견적/정산/배정/리포트 | 상태는 Badge로 항상 노출, 상태별 가능한 액션만 활성화 |

---

## 1. 디자인 토큰(Design Tokens) — “토큰 우선, 하드코딩 금지”

> 모든 색/간격/모션은 아래 토큰 값으로만 구현한다. (로우코드 툴에서는 Theme/Styles 변수로 매핑)

### 1-1. Color Tokens (Light)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| color.bg.base | `#F8FAFC` | 앱 배경 | 페이지 기본 배경(스크롤 영역 포함) |
| color.surface.1 | `#FFFFFF` | 카드/모달/드로어 | “콘텐츠 표면” 기본 |
| color.surface.2 | `#F1F5F9` | 섹션 구분 | 표/리포트의 보조 패널 배경 |
| color.border.default | `#E2E8F0` | 입력/카드/테이블 | 기본 경계선 1px |
| color.border.strong | `#CBD5E1` | 강조 경계 | 구분이 필요한 영역(hover 포함) |
| color.text.primary | `#0F172A` | 본문/제목 | 기본 텍스트(최소 대비 4.5:1) |
| color.text.secondary | `#475569` | 보조 설명 | helper/설명/서브 정보 |
| color.text.muted | `#64748B` | 플레이스홀더 | placeholder/비활성 텍스트 |
| color.text.inverse | `#FFFFFF` | Primary 버튼 등 | 어두운 배경 위 텍스트 |
| color.primary.600 | `#2563EB` | 핵심 CTA | Primary 버튼/링크/포커스 핵심 |
| color.primary.700 | `#1D4ED8` | hover | Primary hover |
| color.primary.800 | `#1E40AF` | active | Primary active |
| color.primary.100 | `#DBEAFE` | 포커스/선택 | focus ring 배경/선택 행 배경 |
| color.success.600 | `#16A34A` | 성공/승인 | Approved/성공 토스트 |
| color.warning.600 | `#D97706` | 경고 | 마진 미달/주의 토스트 |
| color.danger.600 | `#DC2626` | 오류/거절 | 오류/Rejected/차단 메시지 |
| color.info.600 | `#0891B2` | 정보 | 안내/정보 토스트 |
| color.overlay | `#0F172A80` | 모달/드로어 오버레이 | 배경 클릭 차단, 투명도 고정(50%) |

### 1-2. Typography Tokens (다국어 폰트 포함)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| font.family.base | `Noto Sans` 계열 | 전 화면 | 우선순위: Noto Sans KR/Thai/Lao/Vietnamese → Noto Sans → system |
| font.size.h1 | `24px` | 페이지 타이틀 | PC 대시보드 타이틀(줄바꿈 허용) |
| font.size.h2 | `20px` | 섹션 타이틀 | 카드/패널 헤더 |
| font.size.h3 | `18px` | 서브 타이틀 | 탭/모달 타이틀 |
| font.size.body | `14px` | 기본 본문 | 표/폼 기본 크기 |
| font.size.bodyLg | `16px` | 모바일 폼 | 가이드 입력 폼 기본 |
| font.size.caption | `12px` | helper/메타 | 도움말/라벨 보조 |
| font.weight.regular | `400` | 본문 | 기본 |
| font.weight.medium | `500` | 라벨/강조 | 입력 라벨/버튼 |
| font.weight.semibold | `600` | 타이틀 | 과도한 Bold 금지 |
| line.height.tight | `20px` | 테이블/라벨 | 줄간격 고정(가독성 우선) |
| line.height.base | `22px` | 본문 | 다국어 확장 대비(Thai/Lao 포함) |

### 1-3. Spacing Tokens (4px 스케일)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| space.0 | `0px` | 리셋 | 불필요한 공백 금지 |
| space.1 | `4px` | 미세 간격 | 아이콘-텍스트/배지 |
| space.2 | `8px` | 기본 간격 | 폼 요소 내부 간격 |
| space.3 | `12px` | 컨트롤 간격 | 버튼 그룹/필터 |
| space.4 | `16px` | 카드 패딩(기본) | 대부분의 카드/섹션 |
| space.5 | `20px` | 넓은 패딩 | 모달/드로어 기본 |
| space.6 | `24px` | 페이지 패딩(PC) | Workspace padding |
| space.8 | `32px` | 섹션 분리 | 대시보드 섹션 간격 |

### 1-4. Radius Tokens

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| radius.sm | `6px` | 입력/배지 | 작은 요소 |
| radius.md | `8px` | 버튼/입력/테이블 | 기본 라운드 |
| radius.lg | `12px` | 카드 | 카드 기본 |
| radius.xl | `16px` | 모달 | 모달/드로어 상단 |
| radius.pill | `9999px` | 칩/토글 | 완전 원형 |

### 1-5. Shadow Tokens

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| shadow.none | `0px 0px 0px 0px #00000000` | 기본 | 기본은 무그림자 |
| shadow.sm | `0px 1px 2px 0px #0F172A0F` | 카드 hover | 과도한 그림자 금지 |
| shadow.md | `0px 8px 20px -8px #0F172A2E` | 모달/토스트 | overlay 위 요소에만 |
| shadow.lg | `0px 16px 40px -16px #0F172A3D` | 큰 드로어 | 중요 레이어만 |

### 1-6. Motion Tokens

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| motion.duration.fast | `150ms` | hover/active | 마이크로 인터랙션 |
| motion.duration.base | `200ms` | 모달/토스트 | 열기/닫기 기본 |
| motion.duration.slow | `300ms` | 드로어 | 큰 패널 이동 |
| motion.easing.standard | `cubic-bezier(0.2,0,0,1)` | 전환 | 가속-감속 표준 |
| motion.skeleton.loop | `1500ms` | 로딩 스켈레톤 | 번쩍임 금지, 부드럽게 |

### 1-7. Z-Index Tokens

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| z.base | `0` | 일반 레이어 | 기본 |
| z.sticky | `10` | sticky header/table head | sticky끼리 충돌 금지 |
| z.dropdown | `20` | 셀렉트/메뉴 | 오버플로우 위 |
| z.overlay | `30` | 오버레이 | 모달/드로어 배경 |
| z.modal | `40` | 모달/드로어 | 최상단 콘텐츠 |
| z.toast | `50` | 토스트 | 항상 최상단 |

---

## 2. 레이아웃 규칙(PC/모바일 공통)

### 2-1. 브레이크포인트

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| bp.xs | `360px` | 최소 모바일 | 가이드 폼 최저 지원폭 |
| bp.sm | `480px` | 큰 모바일 | 2열 금지(1열 유지) |
| bp.md | `768px` | 태블릿 | 사이드바=오프캔버스 |
| bp.lg | `1024px` | 데스크탑 | 좌측 고정 Sidebar 시작 |
| bp.xl | `1280px` | 와이드 | 테이블 밀도/필터 확장 |
| bp.2xl | `1440px` | 최대 기준 | 컨테이너 max 기준 |

### 2-2. 그리드/컨테이너

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| container.max | `1440px` | PC 본문 | 대시보드는 max 1440, 테이블은 필요 시 fluid |
| page.padding.mobile | `16px` | 모바일 | 좌우 여백 고정 |
| page.padding.tablet | `20px` | 태블릿 | 좌우 여백 고정 |
| page.padding.desktop | `24px` | PC | 좌우 여백 고정 |
| grid.columns.desktop | `12` | PC | 카드/리포트 12컬럼 기반 |
| grid.gutter.mobile | `16px` | 모바일 | 카드 간격 |
| grid.gutter.desktop | `24px` | PC | 섹션 간격 |

### 2-3. 내비게이션(권한/역할 기반)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| sidebar.width | `280px` | PC | 메뉴 텍스트 1줄(줄임표 허용) |
| sidebar.collapsed | `72px` | PC | 아이콘+툴팁 필수 |
| topbar.height | `56px` | PC/모바일 | 검색/알림/언어/통화 토글 위치 |
| mobile.actionbar.height | `64px` | 모바일 폼 | “임시저장/제출” 고정, 스크롤과 분리 |

### 2-4. 스크롤/고정(sticky) 규칙

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| sticky.header | `z=10` | 테이블 헤더 | 헤더 높이 고정, 가려짐 금지 |
| sticky.filters | `top:56px` | 목록 필터 | Topbar 아래에 고정(PC) |
| body.scroll.lock | `true` | 모달 오픈 시 | 배경 스크롤 금지 |
| table.hScroll | `min:360px` | 모바일 테이블 | 컬럼 많은 경우 가로 스크롤 허용(헤더/첫 컬럼 sticky 권장) |

---

## 3. 컴포넌트 규격(필수) — 공통 상태 포함

### 3-1. Button

#### A) 기본 규격

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| Button.height.sm | `32px` | 보조 액션 | 테이블 상단 필터/보조 버튼 |
| Button.height.md | `40px` | 기본 | 대부분의 화면 |
| Button.height.lg | `48px` | 모바일 주요 CTA | 가이드 제출/확정 |
| Button.radius | `8px` | 전 버튼 | radius.md 고정 |
| Button.paddingX.sm | `12px` | sm | 최소 터치 44×44는 별도 보장(모바일) |
| Button.paddingX.md | `16px` | md | 텍스트 1줄 유지 |
| Button.paddingX.lg | `20px` | lg | 긴 라벨은 줄임표+툴팁 |
| Button.icon.size | `16px` | 아이콘 버튼 | 아이콘만 버튼은 44×44px |
| Button.gap | `8px` | 아이콘+텍스트 | 간격 고정 |

#### B) 상태(Variant 공통 룰 + 색상 값 고정)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| Button/상태/default | Primary: bg `#2563EB`, text `#FFFFFF` / Secondary: bg `#FFFFFF`, border `#CBD5E1`, text `#0F172A` | 기본 | Primary=최대 1개/화면, Secondary=보조 |
| Button/상태/hover | Primary bg `#1D4ED8` / Secondary bg `#F8FAFC`, border `#CBD5E1` | 마우스 | hover로 레이아웃 변형 금지 |
| Button/상태/active | Primary bg `#1E40AF` / Secondary bg `#F1F5F9` | 클릭 중 | active는 150ms 이내, 흔들림 금지 |
| Button/상태/focus | ring `2px #DBEAFE`, outline `0px` | 키보드 | 포커스는 항상 보이게(제거 금지) |
| Button/상태/disabled | bg `#E2E8F0`, text `#64748B` | 비활성 | disabled는 클릭/탭 불가 + 이유 툴팁(가능하면) |
| Button/상태/loading | spinner `16px`, text opacity `70%`, duration `200ms` | 비동기 | 로딩 중 중복 클릭 방지(자동 disabled) |
| Button/상태/error | border `1px #DC2626`, shake `150ms`(선택) | 실패 피드백 | 실패는 Toast error + 버튼은 원상 복구(반복 클릭 유도 금지) |

---

### 3-2. Input (Text/Number/Currency 공통)

#### A) 기본 규격

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| Input.height | `40px` | PC 기본 | 테이블 필터/폼 |
| Input.height.mobile | `48px` | 모바일 폼 | 터치 타겟 확보 |
| Input.radius | `8px` | 전 입력 | radius.md |
| Input.paddingX | `12px` | 전 입력 | prefix/suffix 포함 시 `12px` 유지 |
| Input.border | `1px #E2E8F0` | 기본 | 기본 경계선 |
| Label.size | `13px` | 라벨 | weight 500, 필수(*)는 라벨 우측 |
| Helper.size | `12px` | 도움말 | color.text.secondary 사용 |
| Error.size | `12px` | 오류문구 | color.danger.600 사용 |

#### B) 상태

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| Input/상태/default | bg `#FFFFFF`, border `#E2E8F0`, text `#0F172A`, placeholder `#94A3B8` | 기본 | placeholder는 힌트만(값처럼 보이게 금지) |
| Input/상태/hover | border `#CBD5E1` | PC | hover는 border만 변경 |
| Input/상태/active | border `#CBD5E1` | 클릭 직후 | active는 시각 변화 최소(깜빡임 금지) |
| Input/상태/focus | border `#2563EB`, ring `2px #DBEAFE` | 입력 중 | 첫 에러 필드로 자동 스크롤/포커스(제출 시) |
| Input/상태/disabled | bg `#F1F5F9`, text `#64748B`, border `#E2E8F0` | 비활성 | 잠긴 값은 “읽기 전용(Readonly)”와 구분(색/아이콘) |
| Input/상태/loading | suffix spinner `16px`, delay `200ms` | 자동조회 | 200ms 미만 작업은 로딩 표시 생략 |
| Input/상태/error | border `#DC2626`, ring `2px #FECACA`, error text `#DC2626` | 검증 실패 | 에러 문구는 구체적으로(무엇/어떻게) |

#### C) 통화 입력(다통화 필수)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| Currency.prefix.width | `64px` | 통화코드 | `KRW/THB/VND/LAK` 고정폭, 변경은 드롭다운 |
| Currency.decimals | `KRW=0, THB=2, VND=0, LAK=0` | 금액 표시 | 반올림 규칙은 일관(리포트/정산 동일) |
| Currency.format | `1,234.56 THB` | 테이블/요약 | 통화코드 항상 노출(기호만 금지) |

---

### 3-3. Textarea

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| Textarea.minHeight | `96px` | 사유/특이사항 | 기본 3~5줄 |
| Textarea.padding | `12px` | 전 영역 | Input과 동일 |
| Textarea/상태/default | bg `#FFFFFF`, border `#E2E8F0` | 기본 | |
| Textarea/상태/hover | border `#CBD5E1` | PC | |
| Textarea/상태/active | border `#CBD5E1` | 클릭 | |
| Textarea/상태/focus | border `#2563EB`, ring `2px #DBEAFE` | 입력 중 | |
| Textarea/상태/disabled | bg `#F1F5F9`, text `#64748B` | 비활성 | |
| Textarea/상태/loading | spinner `16px`(선택) | AI 요약/자동작성 | 로딩 중 편집 잠금 |
| Textarea/상태/error | border `#DC2626`, ring `2px #FECACA` | 검증 실패 | 글자수/금칙어 규칙도 동일 표기 |

---

### 3-4. Card

#### A) 기본 규격

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| Card.padding | `16px` | 기본 카드 | 정보 묶음/섹션 |
| Card.radius | `12px` | 카드 | radius.lg |
| Card.border | `1px #E2E8F0` | 기본 | 표면 구분 |
| Card.title.size | `16px` | 카드 타이틀 | weight 600 금지(500 권장) |
| Card.gap | `12px` | 내부 간격 | 요소 간격 고정 |

#### B) 상태

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| Card/상태/default | bg `#FFFFFF`, shadow `none` | 기본 | 클릭 가능 여부는 커서/chevron으로 표현 |
| Card/상태/hover | shadow `0px 1px 2px 0px #0F172A0F`, border `#CBD5E1` | PC | hover로 이동/확대 금지 |
| Card/상태/active | border `#2563EB` | 선택 | 선택 카드에는 “선택됨” 배지 |
| Card/상태/focus | ring `2px #DBEAFE` | 키보드 | 카드 전체가 버튼 역할이면 role/button 준수 |
| Card/상태/disabled | opacity `60%` | 비활성 | 클릭/탭 불가 |
| Card/상태/loading | skeleton `1500ms` | 로딩 | 스켈레톤은 실제 레이아웃과 동일 크기 |
| Card/상태/error | border `#DC2626`, bg `#FEF2F2` | 오류 | 카드 내부에 “재시도” 버튼 제공 |

---

### 3-5. Modal

#### A) 기본 규격

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| Modal.overlay | `#0F172A80` | 배경 | 클릭 시 닫힘은 위험 액션일 때 금지(기본 허용) |
| Modal.bg | `#FFFFFF` | 패널 | |
| Modal.radius | `16px` | 모달 | |
| Modal.width.sm | `400px` | 단순 확인 | |
| Modal.width.md | `560px` | 기본 폼 | |
| Modal.width.lg | `720px` | 상세/비교 | |
| Modal.padding | `24px` | 내부 | 모바일은 `16px` |
| Modal.enter | `200ms` | 열기 | fade+scale(미세) |
| Modal.exit | `150ms` | 닫기 | fade |

#### B) 상태

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| Modal/상태/default | open enter `200ms` | 기본 | 오픈 시 첫 포커스=닫기 버튼 또는 첫 입력 |
| Modal/상태/hover | close icon hover bg `#F1F5F9` | PC | 닫기 버튼은 항상 보이게 |
| Modal/상태/active | close active bg `#E2E8F0` | 클릭 | |
| Modal/상태/focus | focus trap `true` | 키보드 | Tab 순환, ESC 닫기(파괴적 액션 제외) |
| Modal/상태/disabled | action buttons disabled | 승인/전송 중 | 진행 중 닫기 제한(선택) |
| Modal/상태/loading | skeleton `1500ms` | 데이터 로딩 | 로딩이 길면 “취소” 허용 |
| Modal/상태/error | error panel bg `#FEF2F2`, retry btn | API 실패 | 에러는 원인+재시도+문의 경로 |

---

### 3-6. Drawer

#### A) 기본 규격

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| Drawer.width.sm | `360px` | 상세 보기 | 우측 슬라이드 |
| Drawer.width.md | `480px` | 편집 폼 | |
| Drawer.width.lg | `640px` | 비교/이력 | |
| Drawer.overlay | `#0F172A80` | 배경 | |
| Drawer.padding | `20px` | 내부 | 모바일은 `16px` |
| Drawer.enter | `300ms` | 열기 | slide-in |
| Drawer.exit | `200ms` | 닫기 | slide-out |

#### B) 상태

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| Drawer/상태/default | open enter `300ms` | 기본 | “상세→편집”은 Drawer 우선(모달 남발 금지) |
| Drawer/상태/hover | close hover bg `#F1F5F9` | PC | |
| Drawer/상태/active | close active bg `#E2E8F0` | 클릭 | |
| Drawer/상태/focus | focus trap `true` | 키보드 | ESC 닫기(필요 시 차단) |
| Drawer/상태/disabled | action disabled | 처리 중 | |
| Drawer/상태/loading | skeleton `1500ms` | 로딩 | |
| Drawer/상태/error | error panel bg `#FEF2F2` | 오류 | 재시도/로그ID 표시 |

---

### 3-7. Toast (In-app)

#### A) 기본 규격

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| Toast.width.max | `360px` | PC | 긴 문구는 2줄 제한 |
| Toast.padding | `12px 14px` | 공통 | |
| Toast.radius | `12px` | 공통 | |
| Toast.shadow | `0px 8px 20px -8px #0F172A2E` | 공통 | |
| Toast.enter | `200ms` | 표시 | slide+fade |
| Toast.duration.base | `4000ms` | info/success | 자동 닫힘 |
| Toast.duration.error | `6000ms` | error | 자동 닫힘(길게) |

#### B) 상태

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| Toast/상태/default | bg `#FFFFFF`, border `#E2E8F0` | 기본 | 한 번에 최대 3개(스택) |
| Toast/상태/hover | timer pause `true` | 마우스 | hover 시 자동닫힘 일시정지 |
| Toast/상태/active | action click | “되돌리기” | 액션은 1개만(복잡 금지) |
| Toast/상태/focus | focusable close btn | 키보드 | aria-live 적용(아래 A11y) |
| Toast/상태/disabled | action disabled | 처리 중 | |
| Toast/상태/loading | progress bar `200ms`(선택) | 업로드 등 | 진행률 표시 가능하면 제공 |
| Toast/상태/error | left accent `#DC2626` | 오류 | 오류는 원인+다음 행동(재시도/문의) 포함 |

---

### 3-8. Table (리스트/리포트 핵심)

#### A) 기본 규격

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| Table.header.height | `44px` | 헤더 | sticky 가능 |
| Table.row.height.dense | `44px` | 기본 리스트 | 관리자 기본(정보 밀도) |
| Table.row.height.comfy | `52px` | 모바일/중요 | 가독성 우선 |
| Table.cell.padding | `12px 16px` | 셀 | 숫자/통화는 우측 정렬 |
| Table.border | `1px #E2E8F0` | 외곽 | |
| Table.header.bg | `#F8FAFC` | 헤더 | 헤더는 항상 대비 확보 |
| Table.row.hover.bg | `#F8FAFC` | hover | |
| Table.row.selected.bg | `#DBEAFE` | 선택 | 선택은 색+체크 아이콘 병행 |
| Table.pagination.height | `56px` | 하단 | 페이지네이션 고정 |

#### B) 상태

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| Table/상태/default | row bg `#FFFFFF` | 기본 | 정렬/필터 상태는 상단에 칩으로 노출 |
| Table/상태/hover | row bg `#F8FAFC` | PC | 행 전체 hover(셀별 hover 금지) |
| Table/상태/active | selected bg `#DBEAFE`, left bar `#2563EB` | 선택 | 선택 행의 핵심 액션(상세/승인) 노출 |
| Table/상태/focus | cell outline `2px #DBEAFE` | 키보드 | 방향키 이동은 선택(툴 지원 시) |
| Table/상태/disabled | opacity `60%` | 잠금 행 | Locked 상태는 행 액션 비활성 |
| Table/상태/loading | skeleton rows `1500ms` | 로딩 | 500행↑ 가상스크롤 권장 |
| Table/상태/error | empty state + retry | 실패 | “데이터 없음”과 “오류”는 문구/아이콘 분리 |

---

## 4. 핵심 UX 규칙(업무 흐름 기준)

### 4-1. 폼/검증(견적·정산 공통)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| 검증 트리거 | onBlur + onSubmit | 입력 폼 | 제출 시 첫 오류 필드로 자동 이동 |
| 필수값 표시 | `*` color `#DC2626` | 라벨 | 필수/선택 명확히 구분 |
| 마이너스/마진 미달 | warning `#D97706` | 견적 | 계산 불가(차단) vs 경고(승인 필요) 구분 |
| 자동계산 표시 | badge bg `#F1F5F9` | 계산 필드 | “자동 계산”은 읽기 전용으로 표현 |
| 임시저장 | autosave `2000ms` debounce | 긴 입력 | 저장 상태(저장됨/저장 중/실패) 상단 표시 |

### 4-2. 승인 플로우 UI(담당→소장→대표)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| 상태 Badge | Approved `#16A34A`, Rejected `#DC2626`, InReview `#0891B2`, Draft `#64748B` | 상세/리스트 | 상태는 제목 옆 + 리스트 컬럼에 항상 표시 |
| 승인 버튼 잠금 | disabled style 적용 | 승인 전 출력 | Approved 전에는 PDF/엑셀 “다운로드/외부전송” 비활성 |
| 변경 이력 | Table row height `44px` | 이력 탭 | 누가/언제/무엇(필드) 변경했는지 필수 |
| 반려 사유 | Textarea min `96px` | Rejected | 반려는 사유 필수, 템플릿 문구 제공 |

### 4-3. 인스펙션 미디어(사진/영상) 연결 UX

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| 미디어 썸네일 | `96px` 정사각 | 라이브러리 | Lazy load, 클릭 시 Drawer 상세 |
| 선택 인디케이터 | ring `2px #DBEAFE` | 선택 | 색만으로 선택 표시 금지(체크 병행) |
| 영상 표기 | play icon `16px` + QR | PDF | PDF에는 링크/QR만(인라인 재생 금지) |
| PDF 미리보기 | A4 `794×1123px` | 견적 출력 | 페이지 분리 규칙 고정(이미지 잘림 금지) |

### 4-4. 다국어 UI(문구 세트) 규칙

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| 언어 토글 위치 | Topbar 우측 | 전 화면 | KR/TH/VI/LO 고정, 사용자 프로필에 기본값 저장 |
| 텍스트 확장 대비 | 여유 `30%` | 버튼/라벨 | 고정폭 버튼 금지(아이콘+툴팁 대체) |
| 날짜/숫자 로캘 | locale 기반 | 리포트/금액 | 언어와 로캘 분리 가능(운영 규칙에 따름) |

### 4-5. 다통화/환율 UX

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| 기준 통화 표시 | badge bg `#F1F5F9` | 요약 카드 | “기준통화: KRW” 항상 노출 |
| 환율 변경 경고 | warning `#D97706` | 설정 | 환율 변경 시 영향 범위(견적/정산/리포트) 안내 후 적용 |
| 자동 재계산 | loading `200ms` | 금액 영역 | 재계산 중 잠금 + 완료 토스트 |

---

## 5. 접근성(A11y) 체크리스트

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| 대비(텍스트) | `4.5:1` 이상 | 전 화면 | 캡션도 동일 기준(예외 금지) |
| 포커스 링 | `2px #DBEAFE` | 인터랙션 | focus 숨김 금지, 키보드 탐색 가능 |
| 터치 타겟 | `44×44px` | 모바일 | 아이콘 버튼/체크박스 포함 |
| 라벨 연결 | label + for | 입력 | 아이콘-only 버튼은 aria-label 필수 |
| 에러 안내 | 인라인 + 요약 | 폼 | 에러는 색만으로 표시 금지(문구+아이콘) |
| 모달/드로어 | focus trap | 레이어 | ESC 닫기, 초기 포커스 지정 |
| 토스트 | aria-live | 알림 | info=polite, error=assertive 권장 |
| 테이블 | header scope | 목록 | 정렬 상태를 텍스트로도 노출 |

---

## 6. UI QA 체크리스트(수용 기준)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| 반응형 점검 | `360/768/1024/1440px` | 전 화면 | 가로 스크롤(불가피한 표 제외) 금지 |
| 상태 점검 | `default/hover/active/focus/disabled/loading/error` | 필수 컴포넌트 | 모든 상태 스타일/동작 확인 |
| 승인 차단 | Approved 전 출력 불가 | 견적/정산 | 다운로드/외부전송 버튼 비활성 확인 |
| 다국어 | KR/TH/VI/LO | 전 화면 | 라벨 overflow/줄바꿈/툴팁 확인 |
| 다통화 | KRW/THB/VND/LAK | 금액 | 반올림/표기/환율 변경 재계산 확인 |
| 대용량 성능 | 500행↑ | 테이블 | 로딩/가상스크롤/필터 지연 확인 |
| 미디어 | 이미지/영상 링크 | PDF | 썸네일/QR/링크 동작 및 파일명 규칙 확인 |
| 오류 처리 | API 실패 | 전 화면 | 에러 상태 화면 + 재시도 + 로그ID 노출 |

---

## 7. 가정(Assumptions)

| 항목 | 값(HEX·px·ms) | 사용처 | 규칙 |
|---|---|---|---|
| 브랜드 컬러/폰트 가이드 부재 | Primary `#2563EB`, Noto Sans 계열 | 전 화면 | 발주사 브랜드 가이드 제공 시 color/token만 교체 |
| 다크모드 범위 제외 | Light only | 전 화면 | 토큰 구조는 확장 가능하게 유지 |
| 아이콘 세트 | 24px 그리드(예: Lucide 계열) | 전 화면 | 로우코드 내장 아이콘 사용 시 크기/선 굵기 일관 유지 |
| PDF 생성 방식 | HTML→PDF(가정) | 견적 | A4 기준 `794×1123px`로 레이아웃 고정 |
| 시간대 표기 | 행사 지역 기준(가정) | 일정/배정 | 설정에서 “기본 시간대” 변경 가능하도록 설계 |
